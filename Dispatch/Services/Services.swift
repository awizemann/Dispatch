// Services/ — actors and service seams: GitClient, the MCP bus (Services/MCPBus/),
// project deletion, icon discovery, path safety, and repo bookmarks. Protocols
// these actors conform to must be declared `nonisolated protocol`
// (SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor is on).
